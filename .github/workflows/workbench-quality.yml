name: Workbench Quality

on:
  workflow_dispatch:

jobs:
  advisory-quality:
    runs-on: ubuntu-latest
    concurrency:
      group: workbench-quality-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore Incursa.OpenAI.Agents.slnx

      - name: Restore tools
        run: dotnet tool restore

      - name: Build
        run: dotnet build Incursa.OpenAI.Agents.slnx --no-restore --configuration Release

      - name: Run quality evidence workflow
        shell: pwsh
        run: pwsh -File scripts/quality/run-quality-evidence.ps1 -NoRestore -NoBuild

      - name: Upload quality artifacts
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: workbench-quality-artifacts
          path: |
            artifacts/test-results/advisory/
            artifacts/coverage/advisory/
            artifacts/quality/library-mutation-summary.md
            artifacts/quality/fuzz-corpus-summary.md
            artifacts/quality/testing/
